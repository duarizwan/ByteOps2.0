/**
 * useToolConnections — shared tool connection state from ToolConnectionsProvider.
 */
export {
    useToolConnections,
    type ToolType,
    type ToolConnection,
} from "@/providers/tool-connections-provider";
